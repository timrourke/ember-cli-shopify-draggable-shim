(function() {
  function vendorModule() {
    'use strict';

    return {
      'default': self['Draggable'],
      __esModule: true,
    };
  }

  define('@shopify/draggable', [], vendorModule);
})();
